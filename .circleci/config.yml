version: 2
jobs:
  build:
    docker:
      - image: circleci/elixir:1.8.2
    working_directory: ~/repo
    steps:
      - checkout
      - run: mix deps.get
      - run: mix local.hex --force
      - run: mix espec
