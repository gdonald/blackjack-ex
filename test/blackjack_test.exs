defmodule BlackjackTest do
  use ExUnit.Case

  describe "Game.main/1" do
    test "runs the game" do
      # This test is commented out in the original spec file
      # The test would require complex IO interaction simulation
    end
  end
end
