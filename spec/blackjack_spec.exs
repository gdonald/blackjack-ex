defmodule BlackjackSpec do
  use ESpec

  describe "Game.main/1" do
    it "runs the game" do
#      expect capture_io(
#               fn ->
#                 Blackjack.main([])
#                 IO.write "s"
#                 IO.write "s"
#                 IO.write "q"
#               end
#             )
#             |> to(eq "")
    end
  end
end
